fragment fullAddress on Address {
    id
    lat
    lon
    description
    metros {
        id
        station
        line
        color
        distance
    }
}

fragment fullPhoto on Photo {
    id
    path
    tags {
        id
        name
    }
}